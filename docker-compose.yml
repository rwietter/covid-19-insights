version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    volumes:
      - '.:/app'
    environment:
      - NODE_ENV=production
      - API_URL=http://covid-insights.ddns.net:5001/graphql
